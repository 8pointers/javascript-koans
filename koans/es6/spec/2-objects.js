describe('objects', function () {
  it('should understand shorthand object literal 1', function () {
    const firstName = 'Myamoto', lastName = 'Musashi', person = {firstName, lastName};
    expect(person).toEqual(__);
  });
});
